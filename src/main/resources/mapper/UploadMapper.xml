<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wuyuncheng.xpress.model.dao.UploadDAO">

    <select id="selectFileDetail" resultType="UploadDetailDTO">
        SELECT upload.upload_id, user.username, upload.filename, upload.mimetype, upload.size, upload.created
        FROM upload
        INNER JOIN user ON upload.author_id = user.user_id
    </select>

</mapper>